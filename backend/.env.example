# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database Configuration - SQLite in production will use a file
# But this could be changed to other databases in production
DATABASE_URL=sqlite:./database.sqlite

# Socket.io Configuration
# In production, this would be your domain or IP
SOCKET_ORIGIN=http://localhost:3000 